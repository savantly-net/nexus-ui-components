import { create } from '@storybook/theming/create';

export default create({
  base: 'light',
  brandTitle: 'Nexus UI',
  brandUrl: 'https://github.com/savantly-net/nexus-ui-components',
  brandTarget: '_self',
});